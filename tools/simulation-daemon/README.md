# Moved

The RocketBox simulated hardware now lives in **`../../simulated-hardware/`**.

```bash
cd ../../simulated-hardware
docker compose up --build
# or: npm install && npm start
```

See that directory’s README for ports and configuration.
