display_name=Alice
team=CAD
receive_status=open
receive_folder=/home/geoff-whittington/Incoming/CAD

# Booth / marketing — UI shows ~7 GiB/s (with jitter) during transfers; USB timing unchanged.
demo_display_mib_s=7168
demo_display_jitter_pct=3

# Session handshake tuning (0 or omit = defaults). Lower gaps = faster start, less margin
# on the no-buffer USB link. accept_reply_delay_ms should exceed accept_ready_gap_ms.
# accept_ready_gap_ms=400
# accept_reply_delay_ms=800
# accept_timeout_sec=60
# ready_timeout_sec=20
# session_header_timeout_ms=2000
# payload_header_timeout_ms=15000
